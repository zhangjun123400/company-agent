# 智慧智能体 · 部署配置指南

## 目录

1. [环境要求](#一环境要求)
2. [快速开始](#二快速开始)
3. [配置说明](#三配置说明)
4. [各平台凭据获取方法](#四各平台凭据获取方法)
5. [启动与验证](#五启动与验证)
6. [部署到服务器](#六部署到服务器)
7. [常见问题](#七常见问题)

---

## 一、环境要求

| 软件 | 版本 | 说明 |
|------|------|------|
| Node.js | v18+ | 运行环境，[下载安装](https://nodejs.org) |
| Git | 任意版本 | 代码管理，[下载安装](https://git-scm.com) |

验证安装：
```bash
node --version   # 应显示 v18.x.x 或更高
npm --version    # 应显示 9.x.x 或更高
```

---

## 二、快速开始

### 1. 获取代码

```bash
git clone https://github.com/zhangjun123400/company-agent.git
cd company-agent
```

### 2. 配置凭据

```bash
# 复制配置模板
cp config.env.example config.env

# 用记事本打开 config.env，按下方说明填写各平台的凭据
notepad config.env
```

### 3. 安装依赖

```bash
npm install
```

### 4. 启动服务

```bash
npm run dev
```

看到以下输出说明启动成功：
```
╔══════════════════════════════════════════╗
║        🧠 智慧智能体 已启动              ║
╚══════════════════════════════════════════╝
```

服务地址：`http://localhost:3456`
健康检查：`http://localhost:3456/health`

---

## 三、配置说明

所有配置集中在 `config.env` 文件中，格式为 `字段名=值`。

### 必填项（必须配置，否则服务无法启动）

| 字段名 | 含义 | 示例值 |
|--------|------|--------|
| `FEISHU_PROJECT_PLUGIN_ID` | 飞书项目插件 ID | `MII_xxxxxxxxxxxx` |
| `FEISHU_PROJECT_PLUGIN_SECRET` | 飞书项目插件密钥 | `0154xxxxxxxxxxxx` |
| `FEISHU_PROJECT_USER_KEY` | 你的飞书项目用户标识 | `7649567855117192178` |
| `FEISHU_APP_ID` | 飞书机器人 App ID | `cli_xxxxxxxxxxxxx` |
| `FEISHU_APP_SECRET` | 飞书机器人 App Secret | `lvnncc4xxxxxxxxxxxxx` |
| `DEEPSEEK_API_KEY` | DeepSeek 大模型 Key | `sk-d7xxxxxxxxxxxxxxxxxxxx` |

### 可选项（有默认值，可不改）

| 字段名 | 含义 | 默认值 |
|--------|------|--------|
| `PROJECT_SPACE_KEY` | 飞书项目空间标识 | `aniwonder` |
| `AI_MODEL` | 大模型类型 | `deepseek` |
| `DEEPSEEK_BASE_URL` | DeepSeek API 地址 | `https://api.deepseek.com` |
| `PRD_REVIEW_TIMEOUT_DAYS` | PRD 超时提醒天数 | `3` |
| `CHECK_CRON` | 定时检查表达式 | `0 9 * * *` |

---

## 四、各平台凭据获取方法

### 4.1 飞书项目插件凭据

```
1. 打开飞书项目网页版 → https://project.feishu.cn
2. 左下角头像 → 开放平台
3. 创建插件（或使用已有插件）
4. 记录 Plugin ID 和 Plugin Secret
5. 填入 config.env 的 FEISHU_PROJECT_PLUGIN_ID 和 FEISHU_PROJECT_PLUGIN_SECRET
```

### 4.2 飞书项目 User Key

```
1. 打开飞书项目网页版
2. 左下角点击你的头像
3. 在弹窗中双击头像区域
4. User Key 已自动复制到剪贴板
5. 填入 config.env 的 FEISHU_PROJECT_USER_KEY
```

### 4.3 飞书机器人应用凭据

```
1. 打开飞书开放平台 → https://open.feishu.cn
2. 创建自建应用（或使用已有应用"智小协"）
3. 在应用首页获取 App ID 和 App Secret
4. 填入 config.env 的 FEISHU_APP_ID 和 FEISHU_APP_SECRET

此应用需要开通以下权限：
  - im:message（发送消息）
  - docx:document（创建文档）
  - wiki:wiki（读取知识库）
  - contact:contact:readonly（查询用户）
  - drive:drive.metadata:readonly（文档授权）
```

### 4.4 DeepSeek API Key

```
1. 打开 DeepSeek 平台 → https://platform.deepseek.com
2. 注册/登录 → API Keys → 创建新 Key
3. 复制 Key（sk- 开头）
4. 填入 config.env 的 DEEPSEEK_API_KEY
```

---

## 五、启动与验证

### 5.1 启动

```bash
npm run dev
```

### 5.2 验证服务正常

浏览器打开：`http://localhost:3456/health`

正常返回：
```json
{
  "status": "ok",
  "service": "智慧智能体",
  "connection": "✅ 正常"
}
```

### 5.3 OAuth 授权 Wiki 读取

首次使用需要授权智小协读取飞书 Wiki 文档：
1. 浏览器打开 `http://localhost:3456/auth/feishu-login`
2. 在飞书授权页面点击「授权」
3. 看到「授权成功」即可

### 5.4 手动触发测试

```bash
# 替换为实际的需求 ID
curl -X POST http://localhost:3456/trigger/new-requirement/7037200783
```

---

## 六、部署到服务器

### 6.1 Linux / macOS 服务器

```bash
# 1. 安装 Node.js v18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 2. 克隆代码
git clone https://github.com/zhangjun123400/company-agent.git
cd company-agent

# 3. 配置
cp config.env.example config.env
nano config.env  # 填入真实凭据

# 4. 安装 & 启动
npm install
npm run build  # 编译
npm start      # 生产模式启动（使用 PM2 守护进程）

# 推荐使用 PM2 保持后台运行
npm install -g pm2
pm2 start dist/index.js --name smart-agent
pm2 save
pm2 startup
```

### 6.2 Windows 服务器

```powershell
# 1. 安装 Node.js（https://nodejs.org）
# 2. 克隆代码
git clone https://github.com/zhangjun123400/company-agent.git
cd company-agent

# 3. 配置
copy config.env.example config.env
notepad config.env

# 4. 安装 & 启动
npm install
npm run build
npm start
```

---

## 七、常见问题

**Q: 启动报错 "缺少必需配置"？**
A: 检查 `config.env` 文件是否存在，必填项是否都已填写。

**Q: 健康检查显示连接失败？**
A: 检查网络是否正常，飞书项目插件是否已安装到目标空间。

**Q: Wiki 读取失败？**
A: 检查 OAuth 授权是否已完成。访问 `http://localhost:3456/auth/feishu-login` 重新授权。

**Q: DeepSeek 分析失败？**
A: 检查 `DEEPSEEK_API_KEY` 是否正确，Key 余额是否充足。

**Q: 端口被占用？**
A: 修改启动命令 `PORT=3000 npm run dev` 使用其他端口。

**Q: 如何更新代码？**
A: `git pull && npm install && npm run dev`（重启服务）
